# Template Tezos backend

## Compile and deploy contracts ( using Truffle )

```bash
    npx truffle deploy --f $CONTRACT_NUMBER_START --to $CONTRACT_NUMBER_END --network NETWORK
```

## Start Api

```bash
    npm run dev
```